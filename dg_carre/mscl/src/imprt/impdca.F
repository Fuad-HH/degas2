      subroutine impdca(a,b,*)
c
c  version : 14.01.87
c
#include "impcom.inc"
      character a(lenw),b(lenw),c*(lenw)
      real*8 d
c=====================================================
c
      call bsend(a,b,lena)
      return
c=====================================================
      entry impdcd(c,d,*)
c=====================================================
      read (c,fd,err=10) d
      return
c=====================================================
      entry impdce(c,e,*)
c=====================================================
      read (c,fe,err=10) e
      return
c=====================================================
      entry impdci(c,i,*)
c=====================================================
      read (c,fi,err=10) i
      return
c=====================================================
 10   return  1
c=====================================================
      end
