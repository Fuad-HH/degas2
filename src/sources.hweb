@* Source definitions.

% $Id$

% $Log$
% Revision 1.4  1995/11/03 15:06:50  karney
% Clean up for release.
%
% Revision 1.3  1995/06/12 13:07:15  karney
% Add in dummy elements into common blocks to avoid complaints from
% Alpha.
%
% Revision 1.2  1995/04/25 22:14:50  karney
% Initial version of sources.web
%
% Revision 1.1  1995/04/12 18:34:24  karney
% Merge in comments to array.hweb and changes for Sherwood '95 meeting.
%
%

\ID{$Id$}

@* Specification of the sources.  At present only surface and volume
sources are handled.

@I vector.hweb

@m so_point 0
@m so_line 1
@m so_surface 2
@m so_volume 3

@ Definition of the common blocks.

@a
      package_init(so)
      define_var_pk(so,source_total,FLOAT)
      define_var_pk(so,so_num,INT)
      define_dummy_pk(so,so_ignore,INT)
      define_dimen_pk(so,source_ind,so_num)
      define_dimen_pk(so,source_ends,0,1)
      define_varp_pk(so,source_type,INT,source_ind)
      define_varp_pk(so,source_current,FLOAT,source_ind)
      define_varp_pk(so,source_element,INT,source_ind)
      define_varp_pk(so,source_pos,FLOAT,vc_ind,source_ends,source_ind)
      
      
@ Common blocks definitions.

@f so_common integer
@m so_common
      so_common_a @;


