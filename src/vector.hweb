@* Header macros for vector.
% $Id$

% $Log$
% Revision 1.8  1995/05/22 22:06:08  karney
% Update for fweb 1.50.  Several minor changes.  Implement collide.web.
%
% Revision 1.7  1995/01/30 21:07:02  karney
% Merge in my changes with Daren's
%
% Revision 1.6  1994/11/21 15:52:04  dstotler
% Interim update associated with ratecalc. ratecalc.hweb and
% xsection.hweb have undergone substantial revision, but should be
% relatively stable from here on. Note also that all of the "ncreada", etc.
% macros have been combined into a single ncread macro. The user is
% responsible for ensuring that dimensions and variables are defined /
% used in the proper order (although the built-in "assert"s should
% help).
%
% Revision 1.5  1994/08/24 15:03:00  karney
% Added degasgeom to read in old degas' geometry spec.
%
% Revision 1.4  1993/10/25 22:37:40  karney
% Protect arg in scale.
%
% Revision 1.3  1993/10/11  19:10:42  karney
% Protect t in vc_xvt
%
% Revision 1.2  1993/09/30  18:43:22  karney
% Pre PET checkin
%
% Revision 1.1  1993/09/23  12:17:11  karney
% Checkin under RCS
%

@ Macro definitions.

\ID{$Id$}

@f vc_common integer
@m vc_common real vector_temp[3]

@m vc_difference(x,y,z)
      $DO(I,1,3) {
          z[I]=x[I]-y[I];
      }

@m vc_add(x,y,z)
      $DO(I,1,3) {
          z[I]=x[I]+y[I];
      }

@m vc_copy(x,y) vc_copy1(x,y)
@m vc_copy1(x,y)
      $DO(I,1,3) {
          y[I]=x[I];
      }

@m vc_product(x,y)
      (x[1]*y[1]+x[2]*y[2]+x[3]*y[3])

@m vc_abs(x)
      sqrt(x[1]^2+x[2]^2+x[3]^2)

@m vc_abs(x)
      sqrt(vc_abs2(x))

@m vc_abs2(x)
      (x[1]^2+x[2]^2+x[3]^2)

@m vc_cross(x,y,z)
      vector_temp[1]=x[2]*y[3]-x[3]*y[2];
      vector_temp[2]=x[3]*y[1]-x[1]*y[3];
      z[3]=x[1]*y[2]-x[2]*y[1];
      z[1]=vector_temp[1];
      z[2]=vector_temp[2]@;

@m vc_unit(x,y)
      vector_temp[1]=vc_abs(x);
      assert(vector_temp[1] > zero);
      vc_scale(one/vector_temp[1],x,y)@;

@m vc_scale(a,x,y)
      $DO(I,1,3) {
         y[I]=(a)*x[I];
      }

@m vc_set(x,a,b,c) x[1]=a;x[2]=b;x[3]=c@;

@m vc_xvt(x,v,t,z)
      $DO(I,1,3) {
         z[I]=x[I]+v[I]*(t);
      }

@m vc_swap(x,y)
      $DO(I,1,3) {
         vector_temp[I]=x[I];
         x[I]=y[I];
         y[I]=vector_temp[I];
      }

@m vc_expand(x)
      x[1],x[2],x[3]

@m vc_dummy(x)
      x

@m vc_decl(x)
      real x[3] @;

@m vc_args(x)
      x[1]

@ Vector index

@a
      package_init(vc)
      define_dimen_pk(vc,vc_ind,3)


